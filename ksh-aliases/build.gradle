dependencies {
    compileOnly project(":ksh-api")
    compileOnly "org.jline:jline:$jlineVersion"
    compileOnly "io.gitlab.arturbosch:kutils:$kutilsVersion"
    testImplementation project(":ksh-test")
    testImplementation "io.gitlab.arturbosch:kutils:$kutilsVersion"
}
